<!-- pages/list/list.wxml -->
<viwe class="container">
    <view class="item"
        wx:for="{{ movies }}" 
        wx:for-item="movie" 
        wx:key="unique">
        <movie-item movie="{{ movie }}"></movie-item>  
    </view>
    
    
</viwe>